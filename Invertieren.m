% Deklaration der Matrix A
A = [1 2; 3 4];

% Berechnung der Inversen von A
% Die Funktion "inv" wird verwendet, um die Inverse der Matrix A zu berechnen.
B = inv(A);

% Ausgabe der Matrix A
disp(A);

% Ausgabe der Inversen von A
disp(B);

% ChatGPT Kommentieren lassen:
    % Deklaration der Matrix A
    % Hier wird die Matrix A mit den Werten 1, 2, 3 und 4 erstellt.
    % Die Matrix hat das Format 2x2.

    % Berechnung der Inversen von A
    % Die Inverse der Matrix A wird mit der Funktion "inv" berechnet.
    % Die Inverse ist eine Matrix, bei der das Produkt von A und der Inversen die Einheitsmatrix ergibt.

    % Ausgabe der Matrix A
    % Die Matrix A wird auf dem Bildschirm ausgegeben.

    % Ausgabe der Inversen von A
    % Die Inverse von A wird auf dem Bildschirm ausgegeben.
