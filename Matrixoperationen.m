% Addition von Matrizen
A = [1 2; 3 4];
B = [5 6; 7 8];
C = A + B;
disp(C);

% Subtraktion von Matrizen
D = A - B;
disp(D);

% Multiplikation von Matrizen
E = A * B;
disp(E);

% Division von Matrizen
F = A / B;
disp(F);


% ChatGPT Kommentieren lassen:
    % Addition von Matrizen
    % Die Matrizen A und B werden elementweise addiert und das Ergebnis in der Matrix C gespeichert.
    % Elemente an gleicher Position in den Matrizen A und B werden addiert.

    % Ausgabe der Matrix C
    % Das Ergebnis der Matrixaddition, die Matrix C, wird auf dem Bildschirm ausgegeben.

    % Subtraktion von Matrizen
    % Die Matrizen A und B werden elementweise subtrahiert und das Ergebnis in der Matrix D gespeichert.
    % Elemente an gleicher Position in den Matrizen A und B werden subtrahiert.

    % Ausgabe der Matrix D
    % Das Ergebnis der Matrixsubtraktion, die Matrix D, wird auf dem Bildschirm ausgegeben.

    % Multiplikation von Matrizen
    % Die Matrixmultiplikation zwischen A und B wird durchgeführt und das Ergebnis in der Matrix E gespeichert.
    % Hierbei werden die entsprechenden Zeilen von A mit den entsprechenden Spalten von B multipliziert und addiert.

    % Ausgabe der Matrix E
    % Das Ergebnis der Matrixmultiplikation, die Matrix E, wird auf dem Bildschirm ausgegeben.

    % Division von Matrizen
    % Die Matrixdivision zwischen A und B wird durchgeführt und das Ergebnis in der Matrix F gespeichert.
    % Hierbei wird die Matrix B mit der Inversen von A multipliziert.

    % Ausgabe der Matrix F
    % Das Ergebnis der Matrixdivision, die Matrix F, wird auf dem Bildschirm ausgegeben.
