% Beispiel: Lineare Abbildung im R^2
% Deklaration der Matrix A, die die lineare Abbildung repräsentiert
A = [1 2; 3 4];

% Deklaration des Vektors v im R^2
v = [1; 2];

% Anwendung der linearen Abbildung auf den Vektor v
w = A * v;

% Ausgabe der Matrix A
disp(A);

% Ausgabe des Vektors v
disp(v);

% Ausgabe des transformierten Vektors w
disp(w);


% ChatGPT Kommentieren lassen:
    % Beispiel: Lineare Abbildung im R^2
    % Hier wird eine lineare Abbildung im zweidimensionalen Raum betrachtet.
    % Die Matrix A repräsentiert die lineare Abbildung.

    % Deklaration der Matrix A
    % Die Matrix A wird mit den Werten 1, 2, 3 und 4 erstellt.
    % Die Matrix hat das Format 2x2.

    % Deklaration des Vektors v im R^2
    % Der Vektor v wird mit den Werten 1 und 2 erstellt.

    % Anwendung der linearen Abbildung auf den Vektor v
    % Der Vektor w wird berechnet, indem die Matrix A mit dem Vektor v multipliziert wird.
    % Dies entspricht der Anwendung der linearen Abbildung auf den Vektor v.

    % Ausgabe der Matrix A
    % Die Matrix A wird auf dem Bildschirm ausgegeben.

    % Ausgabe des Vektors v
    % Der Vektor v wird auf dem Bildschirm ausgegeben.

    % Ausgabe des transformierten Vektors w
    % Der Vektor w, der das Ergebnis der linearen Abbildung von v ist, wird auf dem Bildschirm ausgegeben.
