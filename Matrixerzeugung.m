% Erzeugung einer 3x3-Einheitsmatrix
A = eye(3);
disp(A);

% Erzeugung einer 2x4-Nullmatrix
B = zeros(2, 4);
disp(B);

% Erzeugung einer 4x2-Matrix mit Zufallswerten zwischen 0 und 1
C = rand(4, 2);
disp(C);

% ChatGPT Kommentieren lassen:
    % Erzeugung einer 3x3-Einheitsmatrix
    % Die Funktion "eye" wird verwendet, um eine Einheitsmatrix der Größe 3x3 zu erzeugen.
    % Eine Einheitsmatrix ist eine quadratische Matrix, bei der alle Diagonalelemente den Wert 1 haben und alle anderen Elemente den Wert 0 haben.

    % Ausgabe der Einheitsmatrix A
    % Die erzeugte Einheitsmatrix A wird auf dem Bildschirm ausgegeben.

    % Erzeugung einer 2x4-Nullmatrix
    % Die Funktion "zeros" wird verwendet, um eine Nullmatrix der Größe 2x4 zu erzeugen.
    % Eine Nullmatrix ist eine Matrix, bei der alle Elemente den Wert 0 haben.

    % Ausgabe der Nullmatrix B
    % Die erzeugte Nullmatrix B wird auf dem Bildschirm ausgegeben.

    % Erzeugung einer 4x2-Matrix mit Zufallswerten zwischen 0 und 1
    % Die Funktion "rand" wird verwendet, um eine Matrix der Größe 4x2 mit Zufallswerten zwischen 0 und 1 zu erzeugen.

    % Ausgabe der Zufallsmatrix C
    % Die erzeugte Zufallsmatrix C wird auf dem Bildschirm ausgegeben.
