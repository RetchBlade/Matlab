% Deklaration der Matrix A
A = [1 2; 3 4];

% Berechnung der Determinante von A
det_A = det(A);

% Ausgabe der Determinante
disp(det_A);

%ChatGPT Kommentieren lassen:
    % Deklaration der Matrix A
    % Hier wird die Matrix A mit den Werten 1, 2, 3 und 4 erstellt.
    % Die Matrix hat das Format 2x2.

    % Berechnung der Determinante von A
    % Die Funktion "det" wird verwendet, um die Determinante der Matrix A zu berechnen.
    % Die Determinante ist ein Wert, der Eigenschaften der Matrix beschreibt.

    % Ausgabe der Determinante
    % Die berechnete Determinante wird auf dem Bildschirm ausgegeben.

