% Beispiel: Berechnung der L2-Norm eines Vektors im R^3
% Deklaration des Vektors v im dreidimensionalen Raum
v = [1; 2; 3];

% Berechnung der L2-Norm des Vektors v
norm_v = norm(v);

% Beispiel: Berechnung der L1-Norm eines Vektors im R^3
% Berechnung der L1-Norm des Vektors v
norm_v1 = norm(v, 1);

% Ausgabe der L2-Norm
disp(norm_v);

% Ausgabe der L1-Norm
disp(norm_v1);


% ChatGPT Kommentieren lassen:
    % Beispiel: Berechnung der L2-Norm eines Vektors im R^3
    % Hier wird die L2-Norm (Euklidische Norm) eines Vektors im dreidimensionalen Raum betrachtet.
    % Die L2-Norm eines Vektors entspricht der euklidischen Länge oder dem Betrag des Vektors.

    % Deklaration des Vektors v im dreidimensionalen Raum

    % Berechnung der L2-Norm des Vektors v
    % Die Funktion "norm" wird verwendet, um die L2-Norm des Vektors v zu berechnen.

    % Beispiel: Berechnung der L1-Norm eines Vektors im R^3
    % Hier wird die L1-Norm (Betragssumme) eines Vektors im dreidimensionalen Raum betrachtet.
    % Die L1-Norm eines Vektors entspricht der Summe der absoluten Werte der Elemente des Vektors.

    % Berechnung der L1-Norm des Vektors v
    % Die Funktion "norm" wird verwendet, um die L1-Norm des Vektors v zu berechnen.

    % Ausgabe der L2-Norm
    % Das Ergebnis der Berechnung der L2-Norm, norm_v, wird auf dem Bildschirm ausgegeben.

    % Ausgabe der L1-Norm
    % Das Ergebnis der Berechnung der L1-Norm, norm_v1, wird auf dem Bildschirm ausgegeben.
